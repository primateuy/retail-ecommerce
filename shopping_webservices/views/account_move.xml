<odoo>

<data>

    <record id="view_order_form_inherit_shopping_ws" model="ir.ui.view">
        <field name="name">account.move.form.inherit.shopping.ws</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//notebook" position="inside">
                <page string="Shopping">

                    
                    <group>
                        <field name="estadoEnvio" readonly="1"/>
                        <field name="payment_distribution" invisible="1"/>
                        <!-- <field name="homologacion"/> -->
                        
                    </group>

                    <group invisible="estadoEnvio != 'error'">
                        <field name="ultimo_error_log" readonly="1" string="Ultimo error"/>
                    </group>
                    
                    
                        <widget name="account_move_widget"/>
                    
                    <group>
                         <field name="payment_distribution_complete" readonly="1" invisible="metodoUnico"/>
                    </group>

                </page>
            </xpath>

            <xpath expr="//button[@name='action_register_payment']" position="after">
                <field name="mostrar_botones_declaracion" invisible="1"/>
                <div invisible="not mostrar_botones_declaracion">
                    <field name="metodoUnico" invisible="1"/>
                    <button name="declararVentaUnicoMetodoManual" type="object" 
                            string="Declarar venta" 
                            invisible="not metodoUnico"/>
                    <button name="declararVentaVariosMetodosManual" type="object" 
                            string="Declarar venta varios metodos" 
                            invisible="metodoUnico"/>
                </div>
            </xpath>
        </field>
    </record>
</data>

</odoo>