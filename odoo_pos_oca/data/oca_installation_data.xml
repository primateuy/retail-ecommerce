<?xml version="1.0" encoding="utf-8"?>
<odoo noupdate="1">
    <data>
        <!--
            Nota: pos_payment_method_oca se crea/detecta programáticamente en el
            post_init_hook para evitar que la carga del XML intente reescribir
            campos sobre un registro pre-existente (instalaciones con el módulo
            viejo ``odoo_pos_oca`` monolítico). El hook también registra el
            ir.model.data con xml_id ``pos_payment_method_oca``.

            Los demás registros base (payment.method, payment.provider, journal,
            account.payment.method, ir.config_parameter) los aporta
            ``odoo_pos_oca_core``.
        -->
    </data>
</odoo>
