<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">
    <t
        t-name="pos_forum_employee_controls.SelectionPopup"
        t-inherit="point_of_sale.SelectionPopup"
        t-inherit-mode="extension"
        owl="1"
    >
        <!-- Agregar campo de búsqueda debajo del título -->
        <xpath expr="//div[@class='modal-header drag-handle']" position="after">
            <div class="px-3 pt-2">
                <input
                    type="text"
                    class="form-control"
                    placeholder="Buscar..."
                    t-model="state.searchTerm"
                />
            </div>
        </xpath>

        <!-- Usar la lista filtrada en lugar de props.list -->
        <xpath
            expr="//div[contains(@class, 'selection btn-group-vertical')]"
            position="replace"
        >
            <div class="selection btn-group-vertical justify-content-start w-100 p-3 overflow-y-auto">
                <t t-foreach="filteredList" t-as="item" t-key="item.id">
                    <button
                        type="button"
                        class="selection-item d-flex align-items-center justify-content-between btn btn-lg btn-outline-secondary w-100 p-3 text-start"
                        t-att-class="{ 'selected active': item.isSelected }"
                        t-on-click="() => this.selectItem(item.id)"
                    >
                        <div class="d-flex flex-column">
                            <span t-esc="item.label" />
                            <span
                                t-if="item.item and item.item.identification_id"
                                t-esc="item.item.identification_id"
                            />
                            <span
                                t-elif="item.description"
                                t-esc="item.description"
                            />
                        </div>
                        <i class="oi oi-chevron-right" />
                    </button>
                </t>
            </div>
        </xpath>
    </t>
</templates>

